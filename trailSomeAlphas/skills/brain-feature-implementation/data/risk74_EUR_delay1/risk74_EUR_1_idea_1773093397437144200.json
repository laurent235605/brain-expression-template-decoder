{
    "template": "trade_when(greater(abs(group_zscore(subtract(ts_sum(ts_delta({hfown_pctown},1),20), ts_sum(ts_delta({fund_pct_ownership_encr},1),20)), sector)), 2), group_zscore(subtract(ts_sum(ts_delta({hfown_pctown},1),20), ts_sum(ts_delta({fund_pct_ownership_encr},1),20)), sector), 0)",
    "idea": "极端阈值触发交易：结合模板1的核心信号与条件交易逻辑(trade_when)，仅在行业中性Z-score绝对值超过2倍标准差（极端背离）时开仓，其余区间仓位归零，构建高置信度离散信号，避免微弱信号带来的噪音交易。",
    "expression_list": [
        "trade_when(greater(abs(group_zscore(subtract(ts_sum(ts_delta(rsk74_hfown_pctown,1),20), ts_sum(ts_delta(hedge_fund_pct_ownership_encr,1),20)), sector)), 2), group_zscore(subtract(ts_sum(ts_delta(rsk74_hfown_pctown,1),20), ts_sum(ts_delta(hedge_fund_pct_ownership_encr,1),20)), sector), 0)"
    ]
}