{
    "template": "trade_when(greater(ts_sum(abs(ts_delta(vec_avg({version}), 1)), 20), ts_mean(ts_sum(abs(ts_delta(vec_avg({version}), 1)), 20), 60)), ts_delta(vec_avg({version}), 5) * vec_stddev({version}), 0)",
    "idea": "仅当模板2定义的修订强度高于其60日平均水平（活跃关注期）时，才交易模板1的动量-离散度概念，且将vec_range替换为vec_stddev以获得更稳健的离散度度量。通过trade_when实现条件交易，避免在低分析师关注度时期产生噪音交易，确保信号只在信息流动活跃时生效。",
    "expression_list": [
        "trade_when(greater(ts_sum(abs(ts_delta(vec_avg(anl85_version), 1)), 20), ts_mean(ts_sum(abs(ts_delta(vec_avg(anl85_version), 1)), 20), 60)), ts_delta(vec_avg(anl85_version), 5) * vec_stddev(anl85_version), 0)"
    ]
}