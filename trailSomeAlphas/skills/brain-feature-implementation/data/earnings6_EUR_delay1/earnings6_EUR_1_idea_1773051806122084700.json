{
    "template": "ts_decay_linear(trade_when(greater(abs(vec_avg({estimated_eps})), 0.01), divide(subtract(vec_avg({actual_eps}), vec_avg({estimated_eps})), abs(vec_avg({estimated_eps}))), 0), 5)",
    "idea": "对条件化Surprise应用线性衰减加权(ts_decay_linear)，赋予近期Surprise更高权重，使因子对最新盈利公告反应更灵敏，适用于追求时效性的场景（需注意换手率较ts_mean更高）。",
    "expression_list": [
        "ts_decay_linear(trade_when(greater(abs(vec_avg(ern6_estimated_eps)), 0.01), divide(subtract(vec_avg(ern6_actual_eps), vec_avg(ern6_estimated_eps)), abs(vec_avg(ern6_estimated_eps))), 0), 5)",
        "ts_decay_linear(trade_when(greater(abs(vec_avg(ern6_estimated_eps)), 0.01), divide(subtract(vec_avg(ern6_actual_eps_274), vec_avg(ern6_estimated_eps)), abs(vec_avg(ern6_estimated_eps))), 0), 5)"
    ]
}