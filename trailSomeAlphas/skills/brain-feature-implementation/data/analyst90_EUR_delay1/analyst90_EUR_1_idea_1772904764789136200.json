{
    "template": "trade_when(and(greater(rank(winsorize(vec_avg({likelihood_score}) - ts_mean(vec_avg({likelihood_score}), 30) * ((vec_avg({exit_price}) / vec_avg({openprice})) / ts_mean(vec_avg({exit_price}) / vec_avg({openprice}), 30)), std=4)), 0.8), less(zscore(winsorize(ts_delta(winsorize(ts_std_dev(vec_avg({likelihood_score}), 60), std=4), 20), std=4)), -0.5)), 1.5, trade_when(and(less(rank(winsorize(vec_avg({likelihood_score}) - ts_mean(vec_avg({likelihood_score}), 30) * ((vec_avg({exit_price}) / vec_avg({openprice})) / ts_mean(vec_avg({exit_price}) / vec_avg({openprice}), 30)), std=4)), 0.2), less(zscore(winsorize(ts_delta(winsorize(ts_std_dev(vec_avg({likelihood_score}), 60), std=4), 20), std=4)), -0.5)), -1.5, bucket(rank(winsorize(vec_avg({likelihood_score}) - ts_mean(vec_avg({likelihood_score}), 30) * ((vec_avg({exit_price}) / vec_avg({openprice})) / ts_mean(vec_avg({exit_price}) / vec_avg({openprice}), 30)), std=4)), \"0,1,0.2\")))",
    "idea": "分桶择时策略：将背离度分为5个分位桶（bucket），仅在“高背离+波动率显著下降”或“低背离+波动率显著下降”时建立多空极端头寸，中间区域降仓，实现区间化信号提取。",
    "expression_list": [
        "trade_when(and(greater(rank(winsorize(vec_avg(success_likelihood_score) - ts_mean(vec_avg(success_likelihood_score), 30) * ((vec_avg(expected_exit_price) / vec_avg(anl90_openprice)) / ts_mean(vec_avg(expected_exit_price) / vec_avg(anl90_openprice), 30)), std=4)), 0.8), less(zscore(winsorize(ts_delta(winsorize(ts_std_dev(vec_avg(success_likelihood_score), 60), std=4), 20), std=4)), -0.5)), 1.5, trade_when(and(less(rank(winsorize(vec_avg(success_likelihood_score) - ts_mean(vec_avg(success_likelihood_score), 30) * ((vec_avg(expected_exit_price) / vec_avg(anl90_openprice)) / ts_mean(vec_avg(expected_exit_price) / vec_avg(anl90_openprice), 30)), std=4)), 0.2), less(zscore(winsorize(ts_delta(winsorize(ts_std_dev(vec_avg(success_likelihood_score), 60), std=4), 20), std=4)), -0.5)), -1.5, bucket(rank(winsorize(vec_avg(success_likelihood_score) - ts_mean(vec_avg(success_likelihood_score), 30) * ((vec_avg(expected_exit_price) / vec_avg(anl90_openprice)) / ts_mean(vec_avg(expected_exit_price) / vec_avg(anl90_openprice), 30)), std=4)), \"0,1,0.2\")))"
    ]
}