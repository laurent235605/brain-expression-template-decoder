{
    "template": "multiply(group_zscore(vector_neut(winsorize(ts_backfill({fundreportedequityassets},63),std=4), winsorize(ts_backfill({reported_asset_value},63),std=4)), industry), signed_power(subtract(multiply(group_rank(vector_neut(winsorize(ts_backfill({fundreportedequityassets},63),std=4), winsorize(ts_backfill({reported_asset_value},63),std=4)), industry), 2), 1), 3))",
    "idea": "非线性连续响应替代硬阈值：将行业分位从[0,1]映射到[-1,1]后应用立方变换(signed_power, y=3)，使接近0.5的中间分位权重压缩至接近0，而极端分位(0.9以上或0.1以下)的权重被放大至接近±1，实现平滑的S型响应曲线而非断崖式开关。",
    "expression_list": [
        "multiply(group_zscore(vector_neut(winsorize(ts_backfill(inst16_ipreo_fundreportedequityassets,63),std=4), winsorize(ts_backfill(fund_reported_asset_value,63),std=4)), industry), signed_power(subtract(multiply(group_rank(vector_neut(winsorize(ts_backfill(inst16_ipreo_fundreportedequityassets,63),std=4), winsorize(ts_backfill(fund_reported_asset_value,63),std=4)), industry), 2), 1), 3))"
    ]
}