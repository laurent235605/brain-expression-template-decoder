{
    "template": "ts_decay_linear(trade_when(and(greater(group_rank(vec_range(vec_filter({fiscalyearend}, value=\"nan\")), sector), 0.9), greater(ts_rank(vec_range(vec_filter({fiscalyearend}, value=\"nan\")), 252), 0.8)), signed_power(group_zscore(winsorize(vec_range(vec_filter({fiscalyearend}, value=\"nan\")), std=4), sector), 1.5), 0), 5)",
    "idea": "双极端时序过滤：仅当会计年度分散度同时处于行业前10%截面极值与自身历史252日前20%高位时触发信号，配合5日线性衰减控制换手率，捕捉公司架构复杂度的持续性峰值而非短暂扰动。",
    "expression_list": [
        "ts_decay_linear(trade_when(and(greater(group_rank(vec_range(vec_filter(anl30_fiscalyearend, value=\"nan\")), sector), 0.9), greater(ts_rank(vec_range(vec_filter(anl30_fiscalyearend, value=\"nan\")), 252), 0.8)), signed_power(group_zscore(winsorize(vec_range(vec_filter(anl30_fiscalyearend, value=\"nan\")), std=4), sector), 1.5), 0), 5)"
    ]
}