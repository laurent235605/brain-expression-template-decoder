{
    "template": "trade_when(greater(group_rank(vec_range(vec_filter({fiscalyearend}, value=\"nan\")), sector), 0.85), signed_power(group_zscore(vec_range(vec_filter({fiscalyearend}, value=\"nan\")), sector), 1.5), 0)",
    "idea": "仅在行业内会计年度分散度处于前15%极端位置时才产生信号，并使用signed_power非线性放大极端值，构建'极端会计复杂性'因子；中间范围视为噪音设为0，避免温和分散度的干扰，专注最可能存在财务报告风险或多重子公司架构的标的。",
    "expression_list": [
        "trade_when(greater(group_rank(vec_range(vec_filter(anl30_fiscalyearend, value=\"nan\")), sector), 0.85), signed_power(group_zscore(vec_range(vec_filter(anl30_fiscalyearend, value=\"nan\")), sector), 1.5), 0)"
    ]
}