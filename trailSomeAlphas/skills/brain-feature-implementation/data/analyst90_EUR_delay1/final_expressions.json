[
    "(vec_avg(expected_exit_price) - vec_avg(trade_closing_price)) / (vec_avg(trade_closing_price) + 0.0001)",
    "(vec_avg(buy_recommendation_weighting) - ts_mean(vec_avg(buy_recommendation_weighting), 60)) / ts_std_dev(vec_avg(buy_recommendation_weighting), 60)",
    "(vec_avg(sell_recommendation_weighting) - ts_mean(vec_avg(sell_recommendation_weighting), 60)) / ts_std_dev(vec_avg(sell_recommendation_weighting), 60)",
    "(vec_sum(buy_recommendation_weighting) - vec_sum(buy_recommendation_weighting)) / (vec_sum(buy_recommendation_weighting) + vec_sum(buy_recommendation_weighting) + 0.0001)",
    "(vec_sum(buy_recommendation_weighting) - vec_sum(sell_recommendation_weighting)) / (vec_sum(buy_recommendation_weighting) + vec_sum(sell_recommendation_weighting) + 0.0001)",
    "(vec_sum(sell_recommendation_weighting) - vec_sum(buy_recommendation_weighting)) / (vec_sum(sell_recommendation_weighting) + vec_sum(buy_recommendation_weighting) + 0.0001)",
    "(vec_sum(sell_recommendation_weighting) - vec_sum(sell_recommendation_weighting)) / (vec_sum(sell_recommendation_weighting) + vec_sum(sell_recommendation_weighting) + 0.0001)",
    "not(equal(vec_choose(investment_denomination_currency, nth=0), vec_choose(expected_price_denomination, nth=0)))",
    "quantile(vec_avg(success_likelihood_score), driver=\"uniform\") - ts_delay(quantile(vec_avg(success_likelihood_score), driver=\"uniform\"), 30)",
    "quantile(vec_avg(buy_recommendation_weighting), driver=\"uniform\")",
    "quantile(vec_avg(sell_recommendation_weighting), driver=\"uniform\")",
    "ts_delta((vec_avg(expected_exit_price) - vec_avg(anl90_openprice)) / vec_avg(anl90_openprice), 10)",
    "ts_delta(vec_sum(buy_recommendation_weighting), 5)",
    "ts_delta(vec_sum(sell_recommendation_weighting), 5)",
    "ts_product(1 + vec_avg(success_likelihood_score), 20)",
    "ts_std_dev(vec_avg(success_likelihood_score), 60)",
    "ts_std_dev(vec_avg(buy_recommendation_weighting), 20) / abs(ts_mean(vec_avg(buy_recommendation_weighting), 20))",
    "ts_std_dev(vec_avg(sell_recommendation_weighting), 20) / abs(ts_mean(vec_avg(sell_recommendation_weighting), 20))",
    "ts_sum(vec_sum(buy_recommendation_weighting), 20)",
    "ts_sum(vec_sum(sell_recommendation_weighting), 20)",
    "vec_avg(success_likelihood_score) * sign(vec_sum(buy_recommendation_weighting) - vec_sum(buy_recommendation_weighting))",
    "vec_avg(success_likelihood_score) * sign(vec_sum(buy_recommendation_weighting) - vec_sum(sell_recommendation_weighting))",
    "vec_avg(success_likelihood_score) * sign(vec_sum(sell_recommendation_weighting) - vec_sum(sell_recommendation_weighting))",
    "vec_avg(success_likelihood_score) * sign(vec_sum(sell_recommendation_weighting) - vec_sum(buy_recommendation_weighting))",
    "vec_avg(success_likelihood_score) * vec_avg(buy_recommendation_weighting) / (ts_std_dev(vec_avg(buy_recommendation_weighting), 20) + 0.0001)",
    "vec_avg(success_likelihood_score) * vec_avg(sell_recommendation_weighting) / (ts_std_dev(vec_avg(sell_recommendation_weighting), 20) + 0.0001)",
    "vec_avg(success_likelihood_score) - ts_mean(vec_avg(success_likelihood_score), 30) * ((vec_avg(expected_exit_price) / vec_avg(anl90_openprice)) / ts_mean(vec_avg(expected_exit_price) / vec_avg(anl90_openprice), 30))",
    "vec_count(vec_filter(buy_recommendation_weighting, value=\"nan\")) / (vec_count(anl90_authorcompany) + 0.0001)",
    "vec_count(vec_filter(sell_recommendation_weighting, value=\"nan\")) / (vec_count(anl90_authorcompany) + 0.0001)"
]